# pi-zentao 0.4.0（对照 zentao-cli 0.3.x 重构）实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 subagent-driven-development（推荐）或 executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 对照 zentao-cli 0.3.x 全面重构 pi-zentao：LLM 工具透传本地数据处理能力与新模块，me 概览走 `my` 快路径并并入待办，新增语义化斜杠命令，清理旧 workaround。

**架构：** 保持现有分层 `lib/cli.ts`（子进程+信封解析）→ `lib/schema.ts`（模块白名单+参数构造）→ tools / commands / overview / ui。模块表从 19 扩到 24（issue/risk/meeting/todo），`ZentaoArgs` 增加 pick/filter/sort/search/searchFields/limit/data 透传；me 概览优先 `my bugs/tasks/todos` 一次调用，版本不足（错误码 2010）回退原扫描路径。

**技术栈：** TypeScript（typebox ^1.0.0 运行时校验）、pi 扩展 API（@earendil-works/pi-coding-agent）、vitest、zentao-cli ^0.3.1（子进程调用）。

**规格：** `docs/superpowers/specs/2026-09-23-zentao-cli-0.3-redesign-design.md`（必读；本计划的任务编号与规格章节对应）

## 全局约束

- 依赖地板：`zentao-cli ^0.3.1`（package.json 已锁）；不得使用 0.2.x 不存在的 CLI 参数而不降级。
- 服务器版本底线：目标服务器 22.0，`my` 模块要求 22.5——凡调用新能力必须捕获错误码 `2010`/`E2010` 并优雅降级或友好提示。
- 测试全部 mock `RunFn`（签名 `(args: string[]) => Promise<unknown>`），禁止真实 CLI/服务器调用。
- 错误映射规则：客户端错误码带 `E` 前缀（E1001/E1004/E1006），版本/服务端错误码不带（`2010`、`2005`）——判断时两种形态都要试。
- commit 信息用中文 conventional commits（参照 `git log`：`feat: …`、`test: …`、`docs: …`）。
- 列表页大小常量用 `LIST_PAGE_SIZE = 1000`（schema.ts 导出）；命令卡片列表页大小 200。
- 每个任务结束必须 `npm run typecheck` 与 `npx vitest run` 全绿后才允许 commit。

---

### 任务 1：schema.ts — 新模块白名单 + 本地处理参数透传

**文件：**
- 修改：`src/lib/schema.ts`
- 测试：`tests/schema.test.ts`

- [ ] **步骤 1：编写失败的测试**

在 `tests/schema.test.ts` 追加（沿用文件顶部已有 import，无需新增）：

```ts
describe("buildCliArgs scopedList 模块（issue/risk/meeting）", () => {
  it("issue 列表带 project → projectIssues --projectID", () => {
    expect(buildCliArgs({ module: "issue", action: "list", project: 167 }))
      .toEqual(["issue", "projectIssues", "--projectID=167", "--recPerPage=1000"]);
  });

  it("risk 列表带 executionID → executionRisks --executionID", () => {
    expect(buildCliArgs({ module: "risk", action: "list", executionID: 168 }))
      .toEqual(["risk", "executionRisks", "--executionID=168", "--recPerPage=1000"]);
  });

  it("meeting 列表无 scope → 全局列表", () => {
    expect(buildCliArgs({ module: "meeting", action: "list" }))
      .toEqual(["meeting", "--recPerPage=1000"]);
  });

  it("issue 不支持 delete 动作", () => {
    expect(() => buildCliArgs({ module: "issue", action: "delete", id: 1 })).toThrow(/不支持动作/);
  });
});

describe("buildCliArgs todo 模块", () => {
  it("todo 支持 create，无 list", () => {
    expect(buildCliArgs({ module: "todo", action: "create", fields: { name: "写周报", date: "2026-09-24" } }))
      .toEqual(["todo", "create", "--name=写周报", "--date=2026-09-24"]);
    expect(() => buildCliArgs({ module: "todo", action: "list" })).toThrow(/不支持动作/);
  });
});

describe("buildCliArgs 本地处理参数透传", () => {
  it("list 附加 pick/filter/sort/search/searchFields/limit", () => {
    expect(buildCliArgs({
      module: "bug", action: "list", product: 26,
      pick: "id,title,status",
      filter: ["status=active", "pri<=2"],
      sort: "pri:asc",
      search: "登录",
      searchFields: "title,steps",
      limit: 10,
    })).toEqual([
      "bug", "--product=26", "--recPerPage=1000",
      "--pick=id,title,status",
      "--filter=status=active", "--filter=pri<=2",
      "--sort=pri:asc",
      "--search=登录",
      "--search-fields=title,steps",
      "--limit=10",
    ]);
  });

  it("get 只附加 pick，不附加 list 专属参数", () => {
    expect(buildCliArgs({
      module: "bug", action: "get", id: 42,
      pick: "id,title", filter: ["status=active"], sort: "id:desc", limit: 5,
    })).toEqual(["bug", "42", "--pick=id,title"]);
  });

  it("create/update/状态动作附加 data（JSON 请求体）", () => {
    expect(buildCliArgs({
      module: "story", action: "create",
      fields: { productID: 1, title: "标题" },
      data: '{"reviewer":["a","b"],"spec":"<p>正文</p>"}',
    })).toEqual(["story", "create", "--productID=1", "--title=标题", "--data={\"reviewer\":[\"a\",\"b\"],\"spec\":\"<p>正文</p>\"}"]);
  });

  it("scopedList 模块的 list 也附加处理参数", () => {
    expect(buildCliArgs({ module: "issue", action: "list", project: 167, pick: "id,title", limit: 5 }))
      .toEqual(["issue", "projectIssues", "--projectID=167", "--recPerPage=1000", "--pick=id,title", "--limit=5"]);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run tests/schema.test.ts`
预期：新增 9 个用例 FAIL（`filter`/`pick` 等属性不存在于 `ZentaoArgs`，`issue`/`todo` 是未知模块）。

- [ ] **步骤 3：编写实现代码**

`src/lib/schema.ts` 做四处修改：

(1) `ZentaoArgs` 接口新增字段（放在 `fields` 之后）：

```ts
  fields?: Record<string, string | number | boolean>;
  /** list/get 字段摘取（逗号分隔）。 */
  pick?: string;
  /** list 过滤表达式（元素内逗号=AND，多个元素=OR），如 "status=active,pri<=2"。 */
  filter?: string[];
  /** list 排序，如 "pri:asc,severity:asc"。 */
  sort?: string;
  /** list 搜索关键词。 */
  search?: string;
  /** list 搜索字段（逗号分隔），配合 search。 */
  searchFields?: string;
  /** list 只返回前 N 条。 */
  limit?: number;
  /** create/update/状态动作的 JSON 请求体（嵌套对象/数组/长文本；与 fields 冲突时以 data 为准）。 */
  data?: string;
```

(2) `ModuleInfo` 增加可选字段（放在 `scopeOptional` 之后）：

```ts
  /** scopedList 模块（issue/risk/meeting）：list 按 project/executionID 映射到 projectX/executionX 操作。 */
  readonly scopedList?: { readonly projectOp: string; readonly executionOp: string };
```

(3) `MODULE_INFO` 追加四个模块（放在 `file` 条目之后）：

```ts
  issue:       { label: "问题",   scopeFlag: null, scopedList: { projectOp: "projectIssues", executionOp: "executionIssues" }, actions: set("list", "get", "create", "update") },
  risk:        { label: "风险",   scopeFlag: null, scopedList: { projectOp: "projectRisks", executionOp: "executionRisks" }, actions: set("list", "get", "create", "update") },
  meeting:     { label: "会议",   scopeFlag: null, scopedList: { projectOp: "projectMeetings", executionOp: "executionMeetings" }, actions: set("list", "get", "create", "update", "delete") },
  todo:        { label: "待办",   scopeFlag: null, actions: set("create", "update", "delete") },
```

`ZentaoModule` 联合类型同步追加 `"issue" | "risk" | "meeting" | "todo"`。

(4) `buildCliArgs` 中：
- 在函数开头（`info.actions` 校验之后）加入处理参数构造与 data 构造：

```ts
  const proc: string[] = [];
  if (args.pick !== undefined) proc.push(`--pick=${args.pick}`);
  if (action === "list") {
    for (const f of args.filter ?? []) proc.push(`--filter=${f}`);
    if (args.sort !== undefined) proc.push(`--sort=${args.sort}`);
    if (args.search !== undefined) proc.push(`--search=${args.search}`);
    if (args.searchFields !== undefined) proc.push(`--search-fields=${args.searchFields}`);
    if (args.limit !== undefined) proc.push(`--limit=${args.limit}`);
  }
  const dataArgs = args.data !== undefined ? [`--data=${args.data}`] : [];
```

- list 分支：在校验 `info.actions` 之后、现有 scopeFlag 逻辑之前，插入 scopedList 分支；并把现有三条 return 都追加 `...proc`：

```ts
  if (action === "list") {
    if (info.scopedList !== undefined) {
      if (args.project !== undefined) {
        return [module, info.scopedList.projectOp, `--projectID=${args.project}`, `--recPerPage=${LIST_PAGE_SIZE}`, ...proc];
      }
      if (args.executionID !== undefined) {
        return [module, info.scopedList.executionOp, `--executionID=${args.executionID}`, `--recPerPage=${LIST_PAGE_SIZE}`, ...proc];
      }
      return [module, `--recPerPage=${LIST_PAGE_SIZE}`, ...proc];
    }
    // …以下保持现有逻辑，三条 return 末尾各加 ...proc
```

- get 分支 return 改为：`return [module, String(id), ...proc];`（`--pick` 适用于 get，其余处理参数不进）。
- create/update/delete/resolve/start/finish/末位状态动作六个 return 末尾各加 `...dataArgs`。

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run tests/schema.test.ts`
预期：全部 PASS（含既有用例）。

- [ ] **步骤 5：Commit**

```bash
git add src/lib/schema.ts tests/schema.test.ts
git commit -m "feat: 模块白名单扩至 24（issue/risk/meeting/todo），透传 pick/filter/sort/search/limit/data"
```

---

### 任务 2：cli.ts — 版本不足错误（2010）识别与友好提示

**文件：**
- 修改：`src/lib/cli.ts`
- 测试：`tests/cli.test.ts`

- [ ] **步骤 1：编写失败的测试**

在 `tests/cli.test.ts` 追加（先看文件现有 import 风格，补充 `isVersionUnsupported`、`hintOf` 的 import）：

```ts
describe("isVersionUnsupported", () => {
  const errOf = (code: string): Error & { code: string } => {
    const e = new Error(`zentao: 版本不足`) as Error & { code: string };
    e.code = code;
    return e;
  };

  it("识别 2010 与 E2010（无/带 E 前缀两种形态）", () => {
    expect(isVersionUnsupported(errOf("2010"))).toBe(true);
    expect(isVersionUnsupported(errOf("E2010"))).toBe(true);
  });

  it("其他错误码不误判", () => {
    expect(isVersionUnsupported(errOf("E1001"))).toBe(false);
    expect(isVersionUnsupported(errOf("2005"))).toBe(false);
  });
});

describe("hintOf 版本不足提示", () => {
  it("2010 附带升级/换操作引导", () => {
    const e = new Error("操作 my/todos 不支持当前禅道版本 22.0") as Error & { code: string };
    e.code = "2010";
    expect(hintOf(e)).toMatch(/版本过低|升级禅道/);
    expect(hintOf(e)).toContain("my/todos 不支持当前禅道版本 22.0");
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run tests/cli.test.ts`
预期：2 个新用例 FAIL（`isVersionUnsupported` 未导出；`hintOf` 对 2010 无特殊引导）。

- [ ] **步骤 3：编写实现代码**

`src/lib/cli.ts`：

`asCliError` 之后新增导出函数：

```ts
/** 版本/操作不支持错误（CLI 调用前版本检查）：code 无 E 前缀（2010），兼容带前缀形态（E2010）。 */
export function isVersionUnsupported(error: unknown): boolean {
  const code = asCliError(error).code;
  return code === "2010" || code === "E2010";
}
```

`hintOf` 的 `if` 链最前面（E1001 判断之前）插入：

```ts
  if (e.code === "2010" || e.code === "E2010") {
    return `${e.message}（可升级禅道版本，或改用其他操作/模块）`;
  }
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run tests/cli.test.ts`
预期：全部 PASS。

- [ ] **步骤 5：Commit**

```bash
git add src/lib/cli.ts tests/cli.test.ts
git commit -m "feat: 识别禅道版本不足错误（2010）并给出升级/换操作引导"
```

---

### 任务 3：overview.ts — my 快路径 + 待办合并 + 原生 --sort

**文件：**
- 修改：`src/lib/overview.ts`
- 测试：`tests/overview.test.ts`

- [ ] **步骤 1：编写失败的测试**

在 `tests/overview.test.ts` 追加：

```ts
const errOf = (code: string): Error & { code: string } => {
  const e = new Error(`zentao: ${code}`) as Error & { code: string };
  e.code = code;
  return e;
};

const MY_FAST_RUN = fixtureRun({
  "my bugs": [
    { id: "11", title: "我的Bug", status: "active", pri: 1, product: "26" },
    { id: "12", title: "已解决", status: "resolved", pri: 2, product: "26" },
  ],
  "my tasks": [
    { id: "21", name: "我的任务", status: "doing", pri: 2, execution: "168" },
    { id: "22", name: "已完成", status: "done", pri: 1, execution: "168" },
  ],
  "my todos": [
    { id: "31", name: "写周报", status: "wait", date: "2026-09-24" },
    { id: "32", name: "已完成待办", status: "done", date: "2026-09-23" },
  ],
});

describe("myOverview 快路径（my 模块）", () => {
  it("一次调用取回 bug/task/todo，过滤状态后按 pri 排序", async () => {
    const { items } = await myOverview(MY_FAST_RUN, "zhangsan");
    expect(items.map((i) => i.id)).toEqual(["11", "21", "31"]);
    expect(items[2]).toMatchObject({ kind: "todo", title: "写周报", pri: "", scope: "2026-09-24" });
  });

  it("my todos 报 2010 时跳过待办，bug/task 正常", async () => {
    const run = fixtureRun({
      "my bugs": [{ id: "11", title: "B", status: "active", pri: 1, product: "26" }],
      "my tasks": [],
      "my todos": errOf("2010"),
    });
    const { items } = await myOverview(run, "zhangsan");
    expect(items.map((i) => i.id)).toEqual(["11"]);
  });

  it("my bugs 报 2010 时整体回退到扫描路径", async () => {
    const run = fixtureRun({
      "my bugs": errOf("2010"),
      "profile": { profiles: [{ account: "zhangsan", server: "http://s", current: true }] },
      "product": [{ id: "1", name: "平台", status: "normal" }],
      "bug --product=1": [{ id: "11", title: "我的Bug", status: "active", pri: 1, assignedTo: "zhangsan" }],
      "project": [],
    });
    const { items } = await myOverview(run, "zhangsan");
    expect(items.map((i) => i.id)).toEqual(["11"]);
    expect(items[0]).toMatchObject({ kind: "bug", scope: "平台" });
  });
});
```

注意：`fixtureRun` 当前对非数组返回值直接 `return v`，而第三个用例需要 run 抛错——先把它改成会抛的实现（这本身也是本任务测试基础设施的修正）：

```ts
function fixtureRun(map: Record<string, unknown>): RunFn {
  return async (args) => {
    const key = args.join(" ");
    for (const [k, v] of Object.entries(map)) {
      if (key.startsWith(k)) {
        if (v instanceof Error) throw v;
        return v;
      }
    }
    return [];
  };
}
```

另加一个 scoped 路径原生排序断言（在 SCOPED_RUN 相关 describe 内或新增）：

```ts
it("scoped 任务列表使用原生 --sort=id:desc（不再用 --params orderBy）", async () => {
  const calls: string[][] = [];
  const run: RunFn = async (args) => { calls.push(args); return []; };
  await myOverview(run, "zhangsan", { product: 26, execution: 168 });
  const taskCall = calls.find((c) => c[0] === "task");
  expect(taskCall).toContain("--sort=id:desc");
  expect(taskCall?.join(" ")).not.toContain("--params");
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run tests/overview.test.ts`
预期：新用例 FAIL（`myOverview` 不会调 `my bugs`；scoped 路径仍用 `--params`）。

- [ ] **步骤 3：编写实现代码**

`src/lib/overview.ts`：

(1) import 增加 `isVersionUnsupported`：

```ts
import { currentProfile, isVersionUnsupported, runList, type RunFn } from "./cli.ts";
```

(2) `MyItem` 的 `kind` 扩为 `"bug" | "task" | "todo"`。

(3) `TASK_OPEN` 之后新增待办过滤与快路径函数：

```ts
/** 禅道待办开放状态：done 视为已完成，其余（wait/doing 等）均为待办。 */
const TODO_OPEN = (s: string): boolean => s !== "done";

/** my 快路径：zentao my bugs/tasks/todos 各一次调用（要求服务器 22.5+）。
 *  bugs/tasks 报 2010 → 返回 null（调用方回退扫描）；todos 报 2010 → 跳过待办。 */
async function myOverviewFast(run: RunFn): Promise<OverviewData | null> {
  let bugs: Row[];
  let tasks: Row[];
  try {
    bugs = rows(await runList(run, ["my", "bugs", `--recPerPage=${LIST_PAGE_SIZE}`]));
    tasks = rows(await runList(run, ["my", "tasks", `--recPerPage=${LIST_PAGE_SIZE}`]));
  } catch (err) {
    if (isVersionUnsupported(err)) return null;
    throw err;
  }
  let todos: Row[] = [];
  try {
    todos = rows(await run(["my", "todos", `--recPerPage=${LIST_PAGE_SIZE}`]));
  } catch (err) {
    if (!isVersionUnsupported(err)) throw err;
  }
  const items: MyItem[] = [
    ...bugs.filter((b) => b.status === "active").map((b) => ({
      kind: "bug" as const, id: str(b.id), title: str(b.title), pri: str(b.pri), status: str(b.status), scope: `#${str(b.product)}`,
    })),
    ...tasks.filter((t) => TASK_OPEN.has(str(t.status))).map((t) => ({
      kind: "task" as const, id: str(t.id), title: str(t.name), pri: str(t.pri), status: str(t.status), scope: `#${str(t.execution)}`,
    })),
    ...todos.filter((t) => TODO_OPEN(str(t.status))).map((t) => ({
      kind: "todo" as const, id: str(t.id), title: str(t.name), pri: "", status: str(t.status), scope: str(t.date),
    })),
  ];
  const truncated = [bugs, tasks, todos].some((l) => l.length >= LIST_PAGE_SIZE);
  return { items: sortByPri(items), truncated };
}
```

(4) `myOverview` 函数体最前面（scoped 判断之前）插入：

```ts
  const fast = await myOverviewFast(run);
  if (fast !== null) return fast;
```

（快路径优先于 scoped 路径：它不扫描全量，比 scoped 扫描更少调用。）

(5) `myOverviewScoped` 中任务列表参数，把：

```ts
    const tasks = rows(await runList(run, [
      "task", `--executionID=${context.execution}`, `--recPerPage=${LIST_PAGE_SIZE}`,
      "--params", `{"execution":"${context.execution}","orderBy":"id_desc"}`,
    ]));
```

替换为：

```ts
    // --sort=id:desc：最新任务在前（截断只影响最老数据）；原生替代旧 --params orderBy hack
    const tasks = rows(await runList(run, [
      "task", `--executionID=${context.execution}`, `--recPerPage=${LIST_PAGE_SIZE}`, "--sort=id:desc",
    ]));
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run tests/overview.test.ts`
预期：全部 PASS。若旧用例中 fixtureRun 返回值非 Error 的行为有依赖，修正之（throw 仅对 `instanceof Error` 生效，原有用例返回值均为数组/对象，不受影响）。

- [ ] **步骤 5：Commit**

```bash
git add src/lib/overview.ts tests/overview.test.ts
git commit -m "feat: me 概览走 my 模块快路径并并入禅道待办，任务排序改用原生 --sort"
```

---

### 任务 4：ui.ts — 概览面板渲染 todo 行

**文件：**
- 修改：`src/ui.ts`
- 测试：`tests/ui.test.ts`

- [ ] **步骤 1：编写失败的测试**

在 `tests/ui.test.ts` 追加（沿用文件现有 import）：

```ts
describe("overviewLines me 视图含待办", () => {
  it("todo 项渲染为 [待办]，pri 显示 -，scope 显示日期", () => {
    const lines = overviewLines("me", [
      { kind: "todo", id: "31", title: "写周报", pri: "", status: "wait", scope: "2026-09-24" },
    ]);
    expect(lines[0]).toBe("禅道 · 我的待办");
    expect(lines[1]).toContain("[待办]");
    expect(lines[1]).toContain("#31");
    expect(lines[1]).toContain("P-");
    expect(lines[1]).toContain("2026-09-24");
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run tests/ui.test.ts`
预期：FAIL（`i.kind === "bug" ? "Bug" : "任务"` 把 todo 渲染成"任务"）。

- [ ] **步骤 3：编写实现代码**

`src/ui.ts` 的 `overviewLines` me 分支，把 map 回调改为：

```ts
    const kindLabel = (k: MyItem["kind"]): string => (k === "bug" ? "Bug" : k === "task" ? "任务" : "待办");
    const lines = items.slice(0, 12).map(
      (i) => `  [${kindLabel(i.kind)}] #${i.id} P${i.pri || "-"} ${i.title}（${i.scope}）`,
    );
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run tests/ui.test.ts`
预期：全部 PASS。

- [ ] **步骤 5：Commit**

```bash
git add src/ui.ts tests/ui.test.ts
git commit -m "feat: 概览面板 me 视图渲染禅道待办行"
```

---

### 任务 5：tools/zentao.ts — 注册新参数 + 工具描述更新

**文件：**
- 修改：`src/tools/zentao.ts`
- 测试：`tests/tool-zentao.test.ts`

- [ ] **步骤 1：编写失败的测试**

在 `tests/tool-zentao.test.ts` 追加：

```ts
describe("executeZentao 本地处理参数透传", () => {
  it("list 把 pick/filter/sort/limit 传给 run", async () => {
    const calls: string[][] = [];
    const run: RunFn = async (args) => { calls.push(args); return []; };
    await executeZentao({
      module: "bug", action: "list", product: 26,
      pick: "id,title", filter: ["status=active"], sort: "pri:asc", limit: 5,
    }, run);
    const argv = calls[0] ?? [];
    expect(argv).toContain("--pick=id,title");
    expect(argv).toContain("--filter=status=active");
    expect(argv).toContain("--sort=pri:asc");
    expect(argv).toContain("--limit=5");
  });

  it("create 把 data（JSON 体）传给 run", async () => {
    const calls: string[][] = [];
    const run: RunFn = async (args) => { calls.push(args); return { id: "9" }; };
    await executeZentao({
      module: "story", action: "create",
      fields: { productID: 1, title: "标题" },
      data: '{"reviewer":["a","b"]}',
    }, run);
    expect(calls[0]).toContain('--data={"reviewer":["a","b"]}');
  });

  it("issue list 带 project → projectIssues（端到端经 buildCliArgs）", async () => {
    const calls: string[][] = [];
    const run: RunFn = async (args) => { calls.push(args); return []; };
    await executeZentao({ module: "issue", action: "list", project: 167 }, run);
    expect(calls[0]?.slice(0, 3)).toEqual(["issue", "projectIssues", "--projectID=167"]);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run tests/tool-zentao.test.ts`
预期：3 个新用例 FAIL——工具参数 schema 不含新字段，`executeZentao` 入参被 `params as ZentaoArgs` 直接透传，但 `RunFn` 断言收不到 `--pick` 等（任务 1 已实现 buildCliArgs 透传，故失败点在工具 schema/参数传递：当前 `parameters` 未注册新字段，pi 运行时会剥掉未注册参数——本测试直接调 `executeZentao` 函数体，失败信息应体现在描述更新与否不影响；若意外通过，检查是否走了类型断言跳过了校验，并在步骤 3 中仍补全 schema 与描述）。

- [ ] **步骤 3：编写实现代码**

`src/tools/zentao.ts`：

(1) `parameters` 对象的 `fields` 之后追加：

```ts
      pick: Type.Optional(Type.String({ description: "逗号分隔字段，只返回这些字段（list/get），可大幅减少输出" })),
      filter: Type.Optional(Type.Array(Type.String(), { description: '过滤表达式（如 "status=active,pri<=2"，元素内逗号=AND，多个元素=OR）；仅 list' })),
      sort: Type.Optional(Type.String({ description: "排序，如 pri:asc,severity:asc；仅 list" })),
      search: Type.Optional(Type.String({ description: "搜索关键词；仅 list" })),
      searchFields: Type.Optional(Type.String({ description: "搜索字段（逗号分隔），配合 search；仅 list" })),
      limit: Type.Optional(Type.Number({ description: "只返回前 N 条；仅 list" })),
      data: Type.Optional(Type.String({ description: "JSON 请求体（create/update/状态流转），用于嵌套对象/数组/长文本；与 fields 冲突时以 data 为准" })),
```

(2) `DESCRIPTION` 常量更新——在 "Notes:" 列表中追加三条、并更新第一条列表说明（整段替换为该文本块，以下为要插入的描述行，英文与现有 DESCRIPTION 风格一致）：

```text
- list supports local processing: pick (field selection), filter (e.g. "status=active,pri<=2"; multiple filters = OR), sort ("pri:asc"), search + searchFields, limit. Prefer them over fetching everything — results shrink a lot.
- New in CLI 0.3: issue/risk/meeting lists take project (=projectX op) or executionID (=executionX op), or no scope for a global list; todo supports create/update/delete only.
- Nested writes (arrays, long HTML text): pass data as a JSON string instead of flat fields.
- Version note: if the server is too old for an operation, the CLI fails with code 2010 and a clear message (e.g. my/* requires ZenTao 22.5+).
```

（原有条目全部保留。）

(3) `promptGuidelines` 数组追加一条：

```ts
      "Prefer pick/filter/sort/limit on list actions to keep outputs small; use data (JSON) for nested write payloads.",
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run tests/tool-zentao.test.ts`
预期：全部 PASS。

- [ ] **步骤 5：Commit**

```bash
git add src/tools/zentao.ts tests/tool-zentao.test.ts
git commit -m "feat: zentao 工具注册 pick/filter/sort/search/limit/data 参数并更新描述"
```

---

### 任务 6：commands.ts — 新语义化命令 + 查看命令参数化

**文件：**
- 修改：`src/commands.ts`
- 测试：`tests/commands.test.ts`

- [ ] **步骤 1：编写失败的测试**

在 `tests/commands.test.ts` 追加（import 补充新导出）：

```ts
describe("splitViewArgs", () => {
  it("解析可选 ID 与 --filter/--sort/--pick/--limit 标志", () => {
    expect(splitViewArgs("167 --sort=pri:asc")).toEqual({ id: "167", flags: ["--sort=pri:asc"] });
    expect(splitViewArgs("--filter=status=doing --limit=5")).toEqual({ id: undefined, flags: ["--filter=status=doing", "--limit=5"] });
  });

  it("非法参数返回 null", () => {
    expect(splitViewArgs("--bogus=1")).toBeNull();
    expect(splitViewArgs("167 168")).toBeNull();
    expect(splitViewArgs("abc")).toBeNull();
  });
});

describe("buildScopedModuleArgs（issue/risk/meeting）", () => {
  it("带项目 ID → projectX 操作", () => {
    expect(buildScopedModuleArgs("issue", "167")).toEqual(["issue", "projectIssues", "--projectID=167", "--recPerPage=200"]);
    expect(buildScopedModuleArgs("risk", "167")).toEqual(["risk", "projectRisks", "--projectID=167", "--recPerPage=200"]);
    expect(buildScopedModuleArgs("meeting", "167")).toEqual(["meeting", "projectMeetings", "--projectID=167", "--recPerPage=200"]);
  });

  it("无 ID → 全局列表", () => {
    expect(buildScopedModuleArgs("issue", undefined)).toEqual(["issue", "--recPerPage=200"]);
  });
});

describe("buildMyTodosArgs / buildProductDocsArgs", () => {
  it("我的待办", () => {
    expect(buildMyTodosArgs()).toEqual(["my", "todos", "--recPerPage=200"]);
  });

  it("产品文档：先列库再按库取文档", () => {
    const { libs, docs } = buildProductDocsArgs("26");
    expect(libs).toEqual(["doc", "productLibs", "--productID=26"]);
    expect(docs("7")).toEqual(["doc", "productDocs", "--productID=26", "--libID=7", "--recPerPage=200"]);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run tests/commands.test.ts`
预期：新用例 FAIL（三个构建函数与 splitViewArgs 未导出）。

- [ ] **步骤 3：编写实现代码**

`src/commands.ts`：

(1) 文件顶部（`buildListArgs` 之前）新增：

```ts
/** 查看命令的合法透传标志（新 CLI 本地处理能力）。 */
const VIEW_FLAG_RE = /^--(filter|sort|pick|limit)=/;

/** 解析"查看类"命令参数：至多一个数字 ID + 若干 --filter/--sort/--pick/--limit 标志。
 *  非法输入返回 null（调用方提示用法）。 */
export function splitViewArgs(raw: string): { id: string | undefined; flags: string[] } | null {
  let id: string | undefined;
  const flags: string[] = [];
  for (const a of splitArgs(raw.trim())) {
    if (VIEW_FLAG_RE.test(a)) { flags.push(a); continue; }
    if (id === undefined && isValidId(a)) { id = a; continue; }
    return null;
  }
  return { id, flags };
}

/** issue/risk/meeting：带项目 ID 走 projectX 操作，否则全局列表。 */
export function buildScopedModuleArgs(module: "issue" | "risk" | "meeting", id: string | undefined): string[] {
  const op = { issue: "projectIssues", risk: "projectRisks", meeting: "projectMeetings" }[module];
  return id === undefined
    ? [module, "--recPerPage=200"]
    : [module, op, `--projectID=${id}`, "--recPerPage=200"];
}

/** 我的禅道待办（要求服务器 22.5+，版本不足由 hintOf 提示）。 */
export function buildMyTodosArgs(): string[] {
  return ["my", "todos", "--recPerPage=200"];
}

/** 产品文档：先取文档库列表，再逐个库取文档（productDocs 必填 libID）。 */
export function buildProductDocsArgs(id: string): { libs: string[]; docs: (libId: string) => string[] } {
  return {
    libs: ["doc", "productLibs", `--productID=${id}`],
    docs: (libId) => ["doc", "productDocs", `--productID=${id}`, `--libID=${libId}`, "--recPerPage=200"],
  };
}
```

(2) 参数化现有查看命令——`registerListCommand` 的 handler 改为：

```ts
      handler: async (args, ctx) => {
        const parsed = splitViewArgs(args);
        if (parsed === null) {
          ctx.ui.notify(`用法：/${name} [--filter=…] [--sort=…] [--pick=…] [--limit=N]`, "warning");
          return;
        }
        try {
          const data = await run([...buildListArgs(module, pageSize), ...parsed.flags]);
          pi.appendEntry("zentao-card", { argv: [module], data });
        } catch (err) {
          ctx.ui.notify(hintOf(err), "error");
        }
      },
```

`registerGetCommand` 的 handler 改为：

```ts
      handler: async (args, ctx) => {
        const parsed = splitViewArgs(args);
        if (parsed?.id === undefined) {
          ctx.ui.notify(`用法：/${name} <${module}ID> [--pick=字段1,字段2]，如 /${name} 42`, "warning");
          return;
        }
        try {
          const data = await run([...buildGetArgs(module, parsed.id), ...parsed.flags]);
          pi.appendEntry("zentao-card", { argv: [module, parsed.id], data });
        } catch (err) {
          ctx.ui.notify(hintOf(err), "error");
        }
      },
```

(3) 新命令（放在 `/zentao-executions` 注册之后、"语义化查看命令"注释块之前）。通用渲染复用 `zentao-card`：

```ts
  // /zentao-todos：我的禅道待办（my 模块，22.5+；版本不足友好提示）
  pi.registerCommand("zentao-todos", {
    description: "查看我的禅道待办（要求禅道 22.5+）：/zentao-todos",
    handler: async (_args, ctx) => {
      try {
        const data = await run(buildMyTodosArgs());
        pi.appendEntry("zentao-card", { argv: ["my", "todos"], data });
      } catch (err) {
        ctx.ui.notify(hintOf(err), "error");
      }
    },
  });

  // /zentao-docs <产品ID>：产品下所有文档库中的文档
  pi.registerCommand("zentao-docs", {
    description: "查看产品下所有文档：/zentao-docs <产品ID>",
    handler: async (args, ctx) => {
      const id = args.trim();
      if (!isValidId(id)) {
        ctx.ui.notify("用法：/zentao-docs <产品ID>，如 /zentao-docs 26", "warning");
        return;
      }
      try {
        const { libs, docs } = buildProductDocsArgs(id);
        const libsData = await run(libs);
        const libList = Array.isArray(libsData) ? (libsData as Record<string, unknown>[]) : [];
        if (libList.length === 0) {
          ctx.ui.notify("该产品下没有文档库", "info");
          return;
        }
        const all: Record<string, unknown>[] = [];
        for (const lib of libList) {
          const d = await run(docs(String(lib.id)));
          if (Array.isArray(d)) all.push(...(d as Record<string, unknown>[]));
        }
        pi.appendEntry("zentao-card", { argv: ["doc", "productDocs", `--productID=${id}`], data: all });
      } catch (err) {
        ctx.ui.notify(hintOf(err), "error");
      }
    },
  });

  // issue/risk/meeting 三件套：带项目 ID 走 projectX，无参走全局列表
  const scopedDefs: { cmd: string; module: "issue" | "risk" | "meeting"; label: string }[] = [
    { cmd: "zentao-issues", module: "issue", label: "问题" },
    { cmd: "zentao-risks", module: "risk", label: "风险" },
    { cmd: "zentao-meetings", module: "meeting", label: "会议" },
  ];
  for (const def of scopedDefs) {
    pi.registerCommand(def.cmd, {
      description: `查看${def.label}列表（可选按项目过滤）：/${def.cmd} [项目ID]`,
      handler: async (args, ctx) => {
        const parsed = splitViewArgs(args);
        if (parsed === null || (parsed.id === undefined && parsed.flags.length > 0)) {
          ctx.ui.notify(`用法：/${def.cmd} [项目ID]，如 /${def.cmd} 167`, "warning");
          return;
        }
        try {
          const data = await run(buildScopedModuleArgs(def.module, parsed.id));
          pi.appendEntry("zentao-card", { argv: [def.module], data });
        } catch (err) {
          ctx.ui.notify(hintOf(err), "error");
        }
      },
    });
  }
```

(4) `buildProjectDetailArgs` 的 detail 行上方补注释（实现不变，禅道 API 无 project 详情接口）：

```ts
/** 项目详情 + 下辖执行的并行查询参数。
 *  注意：禅道 API 无 project get-by-id（get project <id> → 2005），
 *  只能 project --search=<id> 后客户端精确匹配——workaround 必须保留。 */
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run tests/commands.test.ts`
预期：全部 PASS。

- [ ] **步骤 5：Commit**

```bash
git add src/commands.ts tests/commands.test.ts
git commit -m "feat: 新增 /zentao-todos /zentao-docs /zentao-issues /zentao-risks /zentao-meetings，查看命令支持 --filter/--sort/--pick/--limit"
```

---

### 任务 7：版本 0.4.0 + README 更新 + 全量验证

**文件：**
- 修改：`package.json`
- 修改：`README.md`

- [ ] **步骤 1：提升版本号**

`package.json` 的 `"version": "0.3.0"` 改为 `"0.4.0"`。

- [ ] **步骤 2：更新 README**

在 README 的功能/命令清单处做三处更新（先读 README 现有结构再动手，保持原有排版风格）：

1. 工具能力段落：补充"zentao 工具支持 pick/filter/sort/search/limit 本地裁剪与 data JSON 写入；模块覆盖 24 个（新增 issue/risk/meeting/todo）"。
2. 命令清单：新增 `/zentao-todos`、`/zentao-docs <产品ID>`、`/zentao-issues [项目ID]`、`/zentao-risks [项目ID]`、`/zentao-meetings [项目ID]`，并注明查看命令支持 `--filter/--sort/--pick/--limit`。
3. 新增"版本要求"小节：`zentao-cli ^0.3.1`；`my` 模块与 issue/risk/meeting 的 projectX 变体要求禅道 22.5+（22.0 会报版本错误，me 概览会自动回退旧路径）。

- [ ] **步骤 3：全量验证**

运行：`npm run verify`
预期：typecheck 通过；85 + 新增用例全部 PASS（预计总数 105±）。

- [ ] **步骤 4：真实环境只读抽查（手动，非自动化）**

在 pi 会话中执行（只读）：
- `/zentao-issues 167` —— 服务器 22.0 预期收到友好提示"最低版本要求 22.5…"（验证 2010 链路）
- `/zentao-todos` —— 同上
- `/zentao-projects --sort=id:desc --limit=5` —— 验证参数化透传

如提示信息不符合预期，回到任务 2/6 修正后重跑 verify。

- [ ] **步骤 5：Commit**

```bash
git add package.json README.md
git commit -m "chore: 版本 0.4.0，README 补充新能力与版本要求"
```

---

## 自检记录

- **规格覆盖度**：规格 §2（工具增强）→ 任务 1/5；§3（me 概览）→ 任务 3/4；§4（命令）→ 任务 6；§5（2010 提示）→ 任务 2；§1/§6（版本与文档/验证）→ 任务 7。无遗漏。
- **占位符扫描**：所有代码步骤均含完整代码；无"适当处理"式描述。任务 5 步骤 2 含一段"若意外通过"的防御性说明，属于测试基线确认而非占位符。
- **类型一致性**：`ZentaoArgs` 新字段（任务 1）与工具 schema（任务 5）、测试断言（任务 1/5）一致；`MyItem["kind"]` 三分支（任务 3）与 ui 渲染（任务 4）一致；`scopedList` 结构体在 schema 定义（任务 1）与 issue/risk/meeting 三条目使用一致；`splitViewArgs`/`buildScopedModuleArgs`/`buildMyTodosArgs`/`buildProductDocsArgs`/`isVersionUnsupported` 的签名与全部调用点一致。
